import random
from replit import clear


def deal_card():


def calculate_scores(cards):
    def compare(user_score, computer_score):
        if user_score == computer_score:
            return "Draw"
        elif computer_score == 0:
            return "lose, opponent has Black Jack"
        elif user_score == 0:
            return "Win , with Black Jack"
        elif user_score > 21:
            return "You went over , You Lose"
        elif computer_score > 21:
            return "Opponent went over , You win"
        elif user_score > computer_score:
            return "You Win"
        else:
            return "You Loose"


def play_game():
    user_cards = []
    computer_cards = []
    is_game_over = False


for _ in range(2):
    user_cards.append(deal_card())
    computer_cards.append(deal
    cards())
    while not is_game_over:
        user_score = calculate_score(user_cards)
        computer_score = calculate_score(computer_cards)
        print(f"your cards : {user_cards} current score : {user_score}")
        print(f"computer 1st card : {computer_cards[0]}")

        if user_score == 0 or computer_score == 0 or user_score > 21:
        else:

        while computer_score != 0 and computer_score < 17:
            computer_cards.append(deal_card())
            computer_score = calculate_score(cpmputer_cards)
        print(f"your_hand : {user_cards} , final score : {user_score}")

        print(f"computers final hand : {computer_cards} final score : {computer_score}")
        print(compare(user_score, computer_score))
        while input("Do ypu want to play Game of Black Jack ? Type'y' or 'n' : ") == "y":
            clear()
            play_game()

import random
from replit import clear


def deal_card():


def calculate_scores(cards):
    def compare(user_score, computer_score):
        if user_score == computer_score:
            return "Draw"
        elif computer_score == 0:
            return "lose, opponent has Black Jack"
        elif user_score == 0:
            return "Win , with Black Jack"
        elif user_score > 21:
            return "You went over , You Lose"
        elif computer_score > 21:
            return "Opponent went over , You win"
        elif user_score > computer_score:
            return "You Win"
        else:
            return "You Loose"


def play_game():
    user_cards = []
    computer_cards = []
    is_game_over = False


for _ in range(2):
    user_cards.append(deal_card())
    computer_cards.append(deal
    cards())
    while not is_game_over:
        user_score = calculate_score(user_cards)
        computer_score = calculate_score(computer_cards)
        print(f"your cards : {user_cards} current score : {user_score}")
        print(f"computer 1st card : {computer_cards[0]}")

        if user_score == 0 or computer_score == 0 or user_score > 21:
        else:

        while computer_score != 0 and computer_score < 17:
            computer_cards.append(deal_card())
            computer_score = calculate_score(cpmputer_cards)
        print(f"your_hand : {user_cards} , final score : {user_score}")

        print(f"computers final hand : {computer_cards} final score : {computer_score}")
        print(compare(user_score, computer_score))
        while input("Do ypu want to play Game of Black Jack ? Type'y' or 'n' : ") == "y":
            clear()
            play_game()

import random
from replit import clear


def deal_card():


def calculate_scores(cards):
    def compare(user_score, computer_score):
        if user_score == computer_score:
            return "Draw"
        elif computer_score == 0:
            return "lose, opponent has Black Jack"
        elif user_score == 0:
            return "Win , with Black Jack"
        elif user_score > 21:
            return "You went over , You Lose"
        elif computer_score > 21:
            return "Opponent went over , You win"
        elif user_score > computer_score:
            return "You Win"
        else:
            return "You Loose"


def play_game():
    user_cards = []
    computer_cards = []
    is_game_over = False


for _ in range(2):
    user_cards.append(deal_card())
    computer_cards.append(deal
    cards())
    while not is_game_over:
        user_score = calculate_score(user_cards)
        computer_score = calculate_score(computer_cards)
        print(f"your cards : {user_cards} current score : {user_score}")
        print(f"computer 1st card : {computer_cards[0]}")

        if user_score == 0 or computer_score == 0 or user_score > 21:
        else:

        while computer_score != 0 and computer_score < 17:
            computer_cards.append(deal_card())
            computer_score = calculate_score(cpmputer_cards)
        print(f"your_hand : {user_cards} , final score : {user_score}")

        print(f"computers final hand : {computer_cards} final score : {computer_score}")
        print(compare(user_score, computer_score))
        while input("Do ypu want to play Game of Black Jack ? Type'y' or 'n' : ") == "y":
            clear()
            play_game()

import random
from replit import clear


def deal_card():


def calculate_scores(cards):
    def compare(user_score, computer_score):
        if user_score == computer_score:
            return "Draw"
        elif computer_score == 0:
            return "lose, opponent has Black Jack"
        elif user_score == 0:
            return "Win , with Black Jack"
        elif user_score > 21:
            return "You went over , You Lose"
        elif computer_score > 21:
            return "Opponent went over , You win"
        elif user_score > computer_score:
            return "You Win"
        else:
            return "You Loose"


def play_game():
    user_cards = []
    computer_cards = []
    is_game_over = False


for _ in range(2):
    user_cards.append(deal_card())
    computer_cards.append(deal
    cards())
    while not is_game_over:
        user_score = calculate_score(user_cards)
        computer_score = calculate_score(computer_cards)
        print(f"your cards : {user_cards} current score : {user_score}")
        print(f"computer 1st card : {computer_cards[0]}")

        if user_score == 0 or computer_score == 0 or user_score > 21:
        else:

        while computer_score != 0 and computer_score < 17:
            computer_cards.append(deal_card())
            computer_score = calculate_score(cpmputer_cards)
        print(f"your_hand : {user_cards} , final score : {user_score}")

        print(f"computers final hand : {computer_cards} final score : {computer_score}")
        print(compare(user_score, computer_score))
        while input("Do ypu want to play Game of Black Jack ? Type'y' or 'n' : ") == "y":
            clear()
            play_game()




